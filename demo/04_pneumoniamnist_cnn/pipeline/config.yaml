# Single source of truth for this scaffolded run (generated by model-scaffold).
task: classification
arch: cnn
seed: 42
id_col: sample_id
in_channels: 1
out_channels: 2
base_channels: 16
split:
  val_frac: 0.15
  test_frac: 0.15
  assignment: splits/split_assignment.csv
  seed_file: splits/split_seed.txt
manifest: _seed_manifest.csv
# --- medsci-skills CNN demo overrides ---
dataset: pneumoniamnist          # MedMNIST v2 (CC BY 4.0; Yang et al., Sci Data 2023)
split: official                  # MedMNIST's predefined train/val/test (image-level benchmark)
loss: cross_entropy              # single-label binary
note: "tooling/methods demonstration on a public benchmark, not a clinical claim"
