This validates installation contracts and documented portability, NOT verified execution inside each host UI. The installer --self-test runs in a temp dir and touches no real host directory. The path-contract scan flags hardcoded personal paths ('/Users/<name>'), distinct from the detectors' generic '/Users/' detection patterns. Host verification status is read from docs/host_compatibility.md (verified 2026-06-03).