Verified ref=HEAD (1d42f478ff27). The archive is a clean committed tree (no working-tree state). pandas was preinstalled in the running interpreter; a true cold-start would `pip install pandas` first. Pass/fail is deterministic; wall-clock varies and is excluded from the reproducibility hash.