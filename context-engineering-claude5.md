# Claude 5세대 모델을 위한 새로운 컨텍스트 엔지니어링 규칙

원문: https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models

## 1. Claude의 제약 풀기 (Unhobbling Claude)

Anthropic은 고급 모델용 Claude Code 시스템 프롬프트의 80% 이상을 제거했지만 성능 저하는 없었다. 기존 방식은 상충하는 지시와 중복 설명으로 모델을 과도하게 제약했는데, 최신 모델은 이런 가이드 없이도 스스로 잘 처리한다.

## 2. 달라진 베스트 프랙티스

| 이전 (Then) | 현재 (Now) |
|---|---|
| Claude에게 엄격한 규칙 부여 | Claude의 판단력 활용 |
| 툴 사용 예시 제공 | 표현력 있는 인터페이스 설계 |
| 모든 정보를 미리 전부 제공 | 점진적 공개(progressive disclosure) |
| 지시를 반복 | 툴 설명(description)에 정보 포함 |
| CLAUDE.md에 메모리 저장 | 자동 메모리 기능 활용 |
| 단순 마크다운 스펙 | 코드 기반의 풍부한 레퍼런스 |

## 3. 핵심 규칙

- **시스템 프롬프트**: 제품 맥락에 맞게 유지. 커스텀 에이전트라면 여기에 투자 많이. 코드 구조에서 추론 가능한 당연한 정보는 명시하지 말 것.
- **CLAUDE.md**: 가볍게 유지. 일반 지식보다 저장소 고유의 비직관적 함정(gotcha)에 집중.
- **Skills**: 팀 고유 의견/베스트 프랙티스를 담은 가벼운 가이드로 취급. 길면 여러 파일로 나눠 점진적 공개 적용.
- **레퍼런스**: 설명보다 코드 기반 스펙 선호. HTML 목업이 디자인 설명 글보다 낫고, 함수 예시가 산문보다 명확한 지시가 됨.

## 4. 실전 권장 사항

- 기존 시스템 프롬프트·CLAUDE.md·스킬을 체계적으로 단순화할 것
- Claude Code의 새 `/doctor` 명령으로 자동 최적화
- 툴은 사용 예시 대신 표현력 있는 파라미터로 설계
- 점진적 공개 구현: 복잡한 컨텍스트는 필요할 때만 로드
- 중복 지시를 인터페이스 설계 개선으로 대체
- 정적 문서에서 동적·코드 기반 레퍼런스로 전환
- 시스템 프롬프트·스킬·사용자 요청 간 상충 지시 점검
